[package]
name = "core-gb"
version = "0.1.0"
edition = "2024"

[dependencies]
core-common = { path = "../core-common" }
serde = { version = "1.0", features = ["derive"] }
bincode = { version = "2.0", features = ["serde"] }
